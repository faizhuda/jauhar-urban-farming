/**
 * `sizes` untuk gambar dalam grid kartu 3 kolom (Products, Gallery, Journal) — lg: kartu
 * ~400px, sm: ~45% lebar viewport (2 kolom), mobile: hampir selebar viewport (1 kolom).
 * Satu sumber supaya breakpoint grid & sizes ini tidak diam-diam berbeda antar halaman.
 */
export const CARD_GRID_SIZES = '(min-width: 1024px) 400px, (min-width: 640px) 45vw, 92vw';
